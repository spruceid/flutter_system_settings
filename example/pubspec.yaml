name: system_settings_example
description: Demonstrates how to use the system_settings plugin.
publish_to: 'none'

environment:
  sdk: '>=3.0.0 < 4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6

dev_dependencies:
  system_settings:
    path: ../

flutter:
  uses-material-design: true
